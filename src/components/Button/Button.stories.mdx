import { MemoryRouter } from 'react-router-dom'
import { Meta, Story, Canvas, Preview, Props } from '@storybook/addon-docs/blocks'
import { Button } from '.'

<Meta title='Components|Button' component={Button} />

# Button
A simple buttons to allow users to carry out an action.
 
## Primary
For use as a main call to action.

<Preview>
  <Story name='Primary'>
    <Button type={'Primary'}>Primary Button</Button>
  </Story>
</Preview>

## Secondary
Secondary buttons should be used sparingly, in instances where a primary button would have too much emphasis.

<Preview>
  <Story name='Secondary'>
    <Button type={'Secondary'}>Secondary Button</Button>
  </Story>
</Preview>

## Tertiary
Tertiary buttons should be used in conjunction with primary buttons where there are two actions.

<Preview>
  <Story name='Tertiary'>
    <Button type={'Tertiary'}>Tertiary Button</Button>
  </Story>
</Preview>

## Icon Button
Icon button

<Preview>
  <Story name='Icon'>
    <div>
      <Button type={'Primary'} icon={{ name: 'Chevron-left', align: 'Left' }}>Icon Button</Button>
      <Button type={'Secondary'} icon={{ name: 'Chevron-right', align: 'Right' }}>Icon Button</Button>
      <Button type={'Tertiary'} icon={{ name: 'Chevron-right', align: 'Right' }}>Icon Button</Button>
    </div>
  </Story>
</Preview>

## Prop Types

<Props of={Button} />

## SCSS Vars

The available Sass [variables](https://sass-lang.com/documentation/at-rules/use#configuring-modules) for this component are:

| Variable | Description |
|:-|:-|
| `$padding` | The left and right padding |
| `$border-radius` | Rounded or square corners |
| `$font-family` | Font family - defaults to base alternative font family |
| `$font-weight` | Font weight - defaults to base font bold weight |
| `$types` | A map containing different button variants |