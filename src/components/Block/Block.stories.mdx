import { MemoryRouter } from 'react-router-dom'
import { Meta, Story, Canvas, Preview, Props } from '@storybook/addon-docs/blocks'
import { Block } from '.'

<Meta title='Components|Block' component={Block} />

# Block
A simple container to put content inside.

## Base
The base block style.

<Preview>
  <Story name='Base'>
    <Block>Primary Block</Block>
  </Story>
</Preview>

## Fill
A block that fills its parent container.

<Preview>
  <Story name='Fill'>
    <Block type={'Fill'}>Primary Block</Block>
  </Story>
</Preview>

## Prop Types

<Props of={Block} />

## SCSS Vars

The available Sass [variables](https://sass-lang.com/documentation/at-rules/use#configuring-modules) for this component are:

| Variable | Description |
|:-|:-|
| `$block-pad` | The padding |
| `$block-border` | The border style |
| `$block-bg-colour` | Background colour |
| `$block-types` | Block variants |