import { Meta, Story, Canvas, Preview, Props } from '@storybook/addon-docs/blocks'
import { Loader } from '.'

<Meta title="Components|Base/Loader" component={Loader} />

# Loader
A simple loader component for displaying loading statues to your users.

## Base
This is the loader in its simplest form.

<Preview>
  <Story name="Base">
    <Loader />
  </Story>
</Preview>

## Variants
Pass a value to the `variant` prop to have your desired loader rendered. `Circle` is the default value, so the prop can be omitted.

<Preview>
  <Story name="Bounce">
    <Loader variant="Bounce" />
  </Story>
</Preview>

## Prop Types
<Props of={Loader} />
