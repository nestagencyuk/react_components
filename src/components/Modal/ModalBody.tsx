import * as React from 'react'
import cx from 'classnames'

/**
 * Modal body
 */
const ModalBody = ({ children }: any) => <div className={cx('modal__body')}>{children}</div>

export default ModalBody
