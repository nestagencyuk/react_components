import { Meta, Story, Canvas, Preview, Props } from '@storybook/addon-docs/blocks'
import { drawerDecorater, demoDecorator } from '../../../.storybook/decorators'
import { Fragment, useContext, useState } from 'react'
import { useToggle } from '../../hooks/useToggle'
import { useStateMachine } from '../../hooks/useStateMachine'
import { opening } from '../../states'
import { Button } from '../Button'
import { Text } from '../Text'
import { Drawer } from '.'

<Meta title="Components|Base/Drawer" component={Drawer} />

# Drawer
A simple drawer that can be used to show/hide content on a specified side of the browser. Compatible with the default state machine.

## Base
This is the drawer in its simplest form.

<Preview>
  <Story name="Toggle">
    {() => {
      const [openState, dispatchEvent] = useStateMachine({ machine: opening })
      return (
        <Fragment>
          <Button onClick={() => dispatchEvent('OPEN')}>Open Drawer</Button>
          <Drawer openState={openState} onClick={() => dispatchEvent('CLOSE')}>
            <Text>
              Item 1
            </Text>
              <Text>
              Item 2
            </Text>
          </Drawer>
        </Fragment>
      )
    }}
  </Story>
</Preview>
