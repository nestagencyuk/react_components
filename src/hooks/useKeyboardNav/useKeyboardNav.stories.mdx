import { Meta, Story, Canvas, Preview, Props } from '@storybook/addon-docs/blocks'
import { RefDiv, RefSection } from '../../../.storybook/helpers'
import { Fragment, useContext, useState, useRef, forwardRef } from 'react'
import { RefBox } from '../../components/Box'
import { List } from '../../components/List'
import { Select } from '../../components/Select'
import { Button } from '../../components/Button'
import { Field } from '../../components/Field'
import { Text } from '../../components/Text'
import { useFocus } from '../useFocus'
import { useKeyboardNav } from '.'

<Meta title="Hooks|Keyboard Navigation" />

# Keyboard Navigation
A hook for cycling through focusable elements using the up/down arrows.

## Base
The hook in its simplest form.

<Preview>
  <Story name="Base">
    {() => {
      const [root, setRoot] = useState(null)
      const [cursor, tabbable, onKeyDown] = useKeyboardNav({ root })
      return (
        <RefSection className="p--md" style={{ border: 'solid 1px #f3f3f3'}}>
          <Text variant="Intro">Normal tabindex</Text>
          <Button className="m--r-md" onClick={() => console.log('Click!')}>Button</Button>
          <Button className="m--r-md" onClick={() => console.log('Click!')}>Button</Button>
          <Button onClick={() => console.log('Click!')}>Button</Button>
          <RefDiv ref={setRoot} className="p--md bg--ui-one m--t-md m--b-md" tabIndex={0} onKeyDown={onKeyDown}>
            <Text variant="Intro">Up/down</Text>
            <List items={[{
              href: '/',
              text: 'Item 1'
            }, {
              href: '/',
              text: 'Item 2'
            }, {
              href: '/',
              icon: {
                align: 'Start',
                name: 'Download'
              },
              text: 'Item 3'
            }]} />
          </RefDiv>
          <Button className="m--r-md" onClick={() => console.log('Click!')}>Button</Button>
          <Button className="m--r-md" onClick={() => console.log('Click!')}>Button</Button>
          <Button onClick={() => console.log('Click!')}>Button</Button>
        </RefSection>
      )
    }}
  </Story>
</Preview>

